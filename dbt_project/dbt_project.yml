name: 'food_waste_analytics'
version: '1.0.0'
config-version: 2

profile: 'food_waste_analytics'

model-paths: ["models"]
seed-paths: ["seeds"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  food_waste_analytics:
    staging:
      +materialized: view
    marts:
      +materialized: table
